import React from 'react'
import { TextField, Grid, InputAdornment, IconButton } from '@material-ui/core'

export default function input() {
  <Grid item xs={12} sm={less ? 6 : 12}>
    <TextField
      name={name}
      onChange={handleChange}
      variant="outlined"
    ></TextField>
  </Grid>
}


